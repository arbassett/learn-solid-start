import { Suspense, type VoidComponent } from "solid-js";
import { A, Head, Title, Meta, Link } from "solid-start";

const Home: VoidComponent = () => {
  return (
    <>
        <h1 class="text-4xl font-bold">Learn Solid Start</h1>
    </>
  );
};

export default Home;
